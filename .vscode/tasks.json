{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "user-fetch",
            "type": "shell",
            "command": "pushd services/user && sls invoke local -f user-fetch --log --path test/user-fetch.json && popd"
        }
    ]
}